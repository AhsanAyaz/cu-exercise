import { IAwesomeTableConfig } from '../interfaces/at-config';

const DEFAULT_AT_CONFIG: IAwesomeTableConfig = {
  headings: [],
  data: [],
  searchEnabled: true,
  searchInputPlaceholder: 'Search'
};

export default DEFAULT_AT_CONFIG;
